# molivarito.github.io

Sitios públicos **construidos automáticamente**. No se editan a mano:

- `acustica-musical/` — Acústica Musical UC. Lo escribe el workflow
  `publish.yml` del repositorio fuente (privado) en cada push a `main`.
